<UserControl
	x:Class="Reversi.Views.Board"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	mc:Ignorable="d"
	d:DesignWidth="640"
	d:DesignHeight="640">
	<Viewbox>
		<Grid>
			<Grid.Resources>
				<Style TargetType="TextBlock">
					<Setter Property="HorizontalAlignment" Value="Center" />
					<Setter Property="VerticalAlignment" Value="Center" />
					<Setter Property="FontSize" Value="40" />
				</Style>
				<!--<Storyboard x:Name="PassStoryboard">
					
				</Storyboard>-->
			</Grid.Resources>
			<Grid x:Name="BoardGrid" />
			<!--<Grid x:Name="PauseDisplay" Visibility="Collapsed">
			</Grid>
			<Grid x:Name="GameOverDisplay" Visibility="Collapsed">
				<TextBlock Text="{Binding GameOverText}" Foreground="Yellow" />
			</Grid>
			<Grid x:Name="PassDisplay" Visibility="Collapsed">
				<TextBlock Text="{Binding PassText}" />
			</Grid>-->
		</Grid>
	</Viewbox>
</UserControl>
